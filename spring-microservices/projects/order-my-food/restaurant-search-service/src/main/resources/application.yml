spring:
  application:
    name: 'restaurant-search-service'

server:
  port: 9000

management:
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    serviceUrl:
      defaultZone: 'http://localhost:8761/eureka/'